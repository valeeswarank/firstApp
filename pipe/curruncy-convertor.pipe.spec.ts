import { CurruncyConvertorPipe } from './curruncy-convertor.pipe';

describe('CurruncyConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new CurruncyConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
